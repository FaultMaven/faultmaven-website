// auth.config.ts
// TODO: Configure NextAuth authentication providers and settings when enabling user authentication.

const authConfig = {
  // Example configuration:
  // providers: [
  //   // Add authentication providers here
  // ],
  // secret: process.env.NEXTAUTH_SECRET,
  // Add other NextAuth.js configuration options
};

export default authConfig;
